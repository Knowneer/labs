# Лабораторная работа №7 — Работа с указателями, ссылками, функциями и лямбда-выражениями

 👥 Участники команды

Иса | Участник 1 |
Вова | Участник 2 |

---

## 🧠 Распределение задач

### 🧑‍💻 Иса (участник 1)
Реализовал функционал обработки оценок:
- `processGrades` — применение операции к каждой оценке через **указатель на функцию**
- Перегрузка `processGrades` с `std::function` для использования **лямбд с захватом**
- Пример лямбды с захватом для нормализации оценок
- Примеры операций: `round_op`, `clamp_0_100`

### 🧑‍💻 Вова (участник 2)
Реализовал работу с фильтрацией и средними значениями:
- `filterStudents` — фильтрация студентов по условию (отличники)
- `calculateAverage` — вычисление среднего с учетом весов через указатель на функцию
- Пример функции-указателя `float (*weight)(int)` и примеры веса (`weight_equal`, `w_linear`)

---



